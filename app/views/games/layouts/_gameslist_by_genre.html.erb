<% @shooter_games.each do |game| %>
<div class="media shadow p-3 mb-3">
  <div class="media-body">
    <h5 class="mt-0 mb-1"><%=game.name%></h5>
    <%=game.summary%>
  </div>
  <%= image_tag(game.cover,class:"ml-3",alt: game.name+"cover_art")%>
  
</div>
<hr>
<% end%> 