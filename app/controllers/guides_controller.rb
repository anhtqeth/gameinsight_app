class GuidesController < ApplicationController
  #THe Guide Feature will be introduced in version 1.1 With integration with CMS Gem
  
  def show
    #Display a grid of card depend on the params input
    #If no guide exist, showing empty html for user to input
  end
  
  def update
    
  end
  
  def find
    #Params should be a name. Similar to a game Search
    #Fetch the guide associated to a game
    #Display in a grid format of card
  end
end
